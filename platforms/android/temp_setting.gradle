android {
            defaultConfig {
                applicationId = "org.nativescript.Native"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }